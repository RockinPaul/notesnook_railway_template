# Notesnook Monograph (published notes) server. Wrapper bakes the Node runtime settings and
# the port Railway routes to for this service.
FROM streetwriters/monograph:1.3.1
ENV NODE_ENV=production \
    HOST=0.0.0.0 \
    PORT=3000
