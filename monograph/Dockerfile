# Notesnook Monograph (published notes) server. Wrapper bakes the Node runtime settings.
# The listen port comes from Railway's injected PORT (8080), which its public domain targets.
FROM streetwriters/monograph:1.3.1
ENV NODE_ENV=production \
    HOST=0.0.0.0
